module AWS.{{= it.mod }} exposing (..)

{-| {{= it.serviceFullName }} ({{= it.apiVersion }})

[JS Docs]({{= it.link }})

@docs apiVersion

## Operations

@docs {{= it.opNames.join(',') }}
-}


{-| Publish date of this version of the API

Format `YYYY-MM-DD`
-}
apiVersion : String
apiVersion =
    "{{= it.apiVersion }}"

{{~ it.ops :op }}
{-| [JS Docs]({{= op.link }})
-}
{{= op.name }} : String
{{= op.name }} =
    "{{= op.name }}"

{{~}}
