module AWS.{{= it.moduleName }} exposing (..)

{-| {{= it.metadata.serviceFullName }} {{= it.apiVersion }}

@docs apiVersion
-}


{-| Publish date of this version of the API

Format `YYYY-MM-DD`
-}
apiVersion : String
apiVersion =
    "{{= it.metadata.apiVersion }}"
